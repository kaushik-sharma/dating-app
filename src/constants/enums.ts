export enum Env {
  DEVELOPMENT = "DEVELOPMENT",
  PRODUCTION = "PRODUCTION",
}

export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum ReactionType {
  LIKE = "LIKE",
  DISLIKE = "DISLIKE",
}
